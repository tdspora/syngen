mixed_complex:
  train_settings:
    source: fixtures/mixed_complex.csv
    epochs: 30
    drop_null: false
  infer_settings:
    size: 600
    random_seed: 10
    run_parallel: false
  keys:
    mixed_complex_pk:
      type: PK
      columns:
      - record_id
