datetime:
  train_settings:
    source: fixtures/datetime.csv
    epochs: 5
    drop_null: false
  infer_settings:
    size: 600
    random_seed: 10
    run_parallel: false
  keys:
    datetime_pk:
      type: PK
      columns:
      - id
