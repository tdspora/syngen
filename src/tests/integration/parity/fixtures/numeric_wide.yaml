numeric_wide:
  train_settings:
    source: fixtures/numeric_wide.csv
    epochs: 5
    drop_null: false
  infer_settings:
    size: 600
    random_seed: 10
    run_parallel: false
  keys:
    numeric_wide_pk:
      type: PK
      columns:
      - id
